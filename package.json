{
  "name": "gestao-projetos",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "mobile:sync": "npm run build && cap sync",
    "mobile:android:add": "npm run build && cap add android",
    "mobile:android:sync": "npm run build && cap sync android",
    "mobile:android:open": "cap open android",
    "mobile:ios:add": "npm run build && cap add ios",
    "mobile:ios:sync": "npm run build && cap sync ios",
    "mobile:ios:open": "cap open ios",
    "mobile:assets": "capacitor-assets generate --ios --android"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/android": "^7.6.1",
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.6.1",
    "@capacitor/core": "^7.6.1",
    "@capacitor/ios": "^7.6.1",
    "@capacitor/local-notifications": "^7.0.6",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
